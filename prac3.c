#include<stdio.h>
int main()
{
int le,bre,area,per;
le = 10;bre =20;
area= le*bre;
per= 2*(le+bre);
printf("area of rectangle : %d\n",area);
printf("permieter of recatngle : %d",per);

    return 0;
}